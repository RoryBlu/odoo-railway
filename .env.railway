# Railway Environment Variables Template for Odoo
# Copy these variables to your Railway dashboard

# === CRITICAL: Volume Permission Fix ===
# This MUST be set for volumes to work correctly
RAILWAY_RUN_UID=101

# === Database Configuration ===
# These are automatically set if you have a PostgreSQL service named "Postgres"
# Otherwise, set them manually
ODOO_DATABASE_HOST=${{Postgres.PGHOST}}
ODOO_DATABASE_PORT=${{Postgres.PGPORT}}
# IMPORTANT: Don't use 'postgres' user - create a dedicated user:
# 1. Connect to PostgreSQL: railway run psql
# 2. Run: CREATE USER odoo WITH CREATEDB PASSWORD 'your-secure-password';
# 3. Update these values:
ODOO_DATABASE_USER=odoo
ODOO_DATABASE_PASSWORD=your-secure-password
ODOO_DATABASE_NAME=${{Postgres.PGDATABASE}}

# === SMTP Configuration (Optional) ===
# Configure these for email functionality
ODOO_SMTP_HOST=
ODOO_SMTP_PORT_NUMBER=
ODOO_SMTP_USER=
ODOO_SMTP_PASSWORD=
ODOO_EMAIL_FROM=

# === Runtime Configuration ===
# PORT is automatically set by Railway, but we set a default
PORT=8069
LOCALE=en_US.UTF-8