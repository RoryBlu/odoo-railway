# Copy and paste these into Railway's Raw Editor
# Click "Raw Editor" button in the Variables tab and paste this entire block
# 
# IMPORTANT SETUP STEPS:
# 1. Add a volume mount in Railway Settings -> Mounts -> Add Mount
#    Mount path: /var/lib/odoo
# 
# 2. Create a dedicated database user (not 'postgres'):
#    - Connect to your PostgreSQL database
#    - Run: CREATE USER odoo WITH CREATEDB PASSWORD 'your-secure-password';
#    - Update ODOO_DATABASE_USER and ODOO_DATABASE_PASSWORD below
#
# 3. If using Railway's default PostgreSQL, you'll need to create the user first
#    or use a custom PostgreSQL instance with a non-postgres user

ODOO_DATABASE_HOST=${{Postgres.PGHOST}}
ODOO_DATABASE_PORT=${{Postgres.PGPORT}}
# IMPORTANT: Change these - Odoo won't run with 'postgres' user for security
ODOO_DATABASE_USER=odoo
ODOO_DATABASE_PASSWORD=your-secure-password
ODOO_DATABASE_NAME=${{Postgres.PGDATABASE}}
PORT=8069
LOCALE=en_US.UTF-8
ODOO_SMTP_HOST=smtp.gmail.com
ODOO_SMTP_PORT_NUMBER=587
ODOO_SMTP_USER=
ODOO_SMTP_PASSWORD=
ODOO_EMAIL_FROM=noreply@example.com