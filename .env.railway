# Railway Environment Variables Template
# Copy and paste these into Railway's Raw Editor
# Click "Raw Editor" button in the Variables tab and paste this entire block
# 
# CRITICAL SETUP STEPS:
# 1. Create a dedicated database user (not 'postgres'):
#    Connect to your PostgreSQL and run:
#    CREATE USER odoo WITH CREATEDB PASSWORD 'your-secure-password';
#    GRANT ALL PRIVILEGES ON DATABASE railway TO odoo;
# 
# 2. Add a volume mount in Railway Settings:
#    - Go to Settings -> Mounts -> Add Mount
#    - Mount path: /var/lib/odoo
# 
# 3. If volume permissions fail, add:
#    RAILWAY_RUN_UID=101
#    (This makes Railway mount the volume with odoo user permissions)

# PostgreSQL Configuration (auto-references Railway PostgreSQL service)
ODOO_DATABASE_HOST=${{Postgres.PGHOST}}
ODOO_DATABASE_PORT=${{Postgres.PGPORT}}
# IMPORTANT: Create this user in PostgreSQL first!
ODOO_DATABASE_USER=odoo
ODOO_DATABASE_PASSWORD=your-secure-password
ODOO_DATABASE_NAME=${{Postgres.PGDATABASE}}

# Runtime Configuration
PORT=8069
LOCALE=en_US.UTF-8

# SMTP Configuration (optional - leave empty to skip)
ODOO_SMTP_HOST=
ODOO_SMTP_PORT_NUMBER=
ODOO_SMTP_USER=
ODOO_SMTP_PASSWORD=
ODOO_EMAIL_FROM=

# Volume Permission Fix (uncomment if needed)
# RAILWAY_RUN_UID=101