# Railway Environment Variables Template for Odoo
# Copy these to Railway's environment variables section

# Database Configuration (Using standard Odoo Docker variables)
HOST=${{Postgres.PGHOST}}
PORT=${{Postgres.PGPORT}}
USER=odoo
PASSWORD=OdooRailway2024!

# Optional: Database name (defaults to 'postgres')
POSTGRES_DB=${{Postgres.PGDATABASE}}

# Optional: Set admin password (otherwise set on first login)
# ADMIN_PASSWORD=your-admin-password

# Note: SMTP configuration can be done through Odoo's web interface after deployment
# Or you can pass SMTP settings as command-line arguments if needed