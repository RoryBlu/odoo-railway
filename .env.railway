# Railway Environment Variables for Odoo
# Copy ALL of these to Railway's environment variables section

# CRITICAL: Tell Railway what port Odoo is listening on
PORT=8069

# Database Configuration (Using standard Odoo Docker variables)
HOST=${{Postgres.PGHOST}}
USER=odoo
PASSWORD=OdooRailway2024!

# Optional: Database name (defaults to 'postgres')
POSTGRES_DB=${{Postgres.PGDATABASE}}

# SMTP Configuration (from your current setup)
ODOO_EMAIL_FROM=Odoo <rory@blucanarycapital.com>
ODOO_SMTP_HOST=smtp.gmail.com
ODOO_SMTP_PASSWORD=lgpu hprz cqfg hzah
ODOO_SMTP_PORT_NUMBER=587
ODOO_SMTP_USER=rory@blucanarycapital.com