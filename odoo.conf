[options]
; Network interface binding - empty listens on all interfaces (0.0.0.0)
http_interface =
http_port = 8080

; Proxy mode for Railway
proxy_mode = True

; Database filter (allow any database name)
dbfilter = .*

; Disable demo data
without_demo = all
